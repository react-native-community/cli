{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "cd packages/local-cli && yarn jest; cd ../..",
    "flow-check": "cd packages/local-cli && yarn flow check; cd ../.."
  }
}
